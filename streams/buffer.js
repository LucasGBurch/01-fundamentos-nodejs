// Buffer: representação de um espaço na memória do computador

const buf = Buffer.from('hello');

console.log(buf);
// node streams/buffer.js retorna a representação hexadecimal da string escrita no buffer (buf.toJSON()) retorna em base decimal